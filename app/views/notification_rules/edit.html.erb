<div class="page-header">
  <div>
    <div class="breadcrumb">
      <%= link_to @project.name, project_path(@project) %> &rsaquo;
      <%= link_to "Settings", settings_project_path(@project) %> &rsaquo; Edit Rule
    </div>
    <h1>Edit Notification Rule</h1>
  </div>
</div>

<%= render "form", project: @project, notification_rule: @notification_rule %>
